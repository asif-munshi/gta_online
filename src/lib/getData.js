export const Agency = (value) => value.attr.ct36.value == "the-agency" && value;

export const Apartments = (value) =>
  value.attr.ct36.value == "apartments" && value;

export const ArenaWorkshop = (value) =>
  value.attr.ct36.value == "arena-workshop" && value;

export const AutoShop = (value) =>
  value.attr.ct36.value == "auto-shop" && value;

export const Bunker = (value) => value.attr.ct36.value == "bunkers" && value;

export const CargoWarehouse = (value) =>
  value.attr.ct36.value == "warehouses" && value;

export const CasinoPenthouse = (value) =>
  value.attr.ct36.value == "master-penthouse" && value;

export const ExecutiveOffice = (value) =>
  value.attr.ct36.value == "offices" && value;

export const ExecutiveOfficeGarages = (value) =>
  value.attr.ct36.value == "office-garages" && value;

export const Facility = (value) =>
  value.attr.ct36.value == "facilities" && value;

export const Garage = (value) => value.attr.ct36.value == "garages" && value;

export const Hangar = (value) => value.attr.ct36.value == "hangars" && value;

export const LargeVehicles = (value) =>
  value.attr.ct36.value == "command-centers" && value;

export const MCBusinesses = (value) =>
  value.attr.ct36.value == "mc-businesses" && value;

export const MCClubhouse = (value) =>
  value.attr.ct36.value == "mc-clubhouses" && value;

export const Nightclub = (value) =>
  value.attr.ct36.value == "nightclubs" && value;

export const RetroArcade = (value) =>
  value.attr.ct36.value == "retro-arcade-property" && value;

export const VehicleWarehouse = (value) =>
  value.attr.ct36.value == "vehicle-warehouses" && value;

export const Yacht = (value) =>
  value.attr.ct36.value == "galaxy-super-yacht" && value;
